
import { links } from "$lib/server/redirect";
import type { RequestHandler } from "@sveltejs/kit";
import { error, redirect } from '@sveltejs/kit';
export { robotstxtGET as GET } from '$lib/server/api';

